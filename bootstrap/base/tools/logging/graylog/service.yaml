apiVersion: v1
kind: Service
metadata:
  name: graylog-service
  namespace: logging
spec:
  selector:
    app: graylog
  type: NodePort
  ports:
    - protocol: TCP
      port: 9000
      nodePort: 30084