apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Bootstrap a k8s cluster with basic components, tools, databases, and more.
# Add more as needed :) 
# Run "kustomize build base" to deploy

resources:
  - db
  - mon
  - net
  - tools